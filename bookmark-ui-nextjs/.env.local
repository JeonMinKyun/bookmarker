#NEXT_PUBLIC_ 접두어를 붙여서 클라이언트와 서버 모두에서 접근할 수 있게 할 수 있다

#add/page.tsx 에서 호출 되고 ('use client'가 선언되어 있다 - CSR)
NEXT_PUBLIC_CLIENT_SIDE_API_BASE_URL=http://localhost:18080

#page.tsx에서 호출 된다(SSR)
SERVER_SIDE_API_BASE_URL=http://bookmarker-api:8080